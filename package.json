{
  "name": "my-nodejs-app",
  "version": "1.0.0",
  "description": "A sample Node.js app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'Building the app...' && mkdir -p build && rsync -av --progress --exclude=build ./ build/"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
